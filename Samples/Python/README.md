# README

## Requirements

This project was tested using Python `3.11.9`.

## Setup

To run the Jupyter Notebook scripts, create a new virtual environment:

```bash
python -m venv .venv
```

Then install the required dependencies:

```bash
.venv\Scripts\pip install -r requirements.txt
```
